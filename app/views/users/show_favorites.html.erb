<div class="row">
  <div class="col-md-8 col-md-push-4">
    <%= render 'follow_form' if logged_in? %>
    <h3><span class="glyphicon glyphicon-heart" aria-hidden="true"></span>スキなボイス</h3>
    <% if @user.voices.any? %>
      <ol class="voices">
        <%= render @voices %>
      </ol>
    <% end %>
    <%= paginate @voices %>
  </div>
  <%= render 'aside2' %>
</div>